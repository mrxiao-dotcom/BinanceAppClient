<Window x:Class="BinanceApps.WPF.MarketDistributionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="市场每日涨幅分布情况" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" 
                BorderThickness="0,0,0,1" Padding="15,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊 市场每日涨幅分布" FontSize="18" 
                           FontWeight="Bold" VerticalAlignment="Center" 
                           Foreground="#333" Margin="0,0,20,0"/>
                
                <Button x:Name="btnRefresh" Content="🔄 刷新数据" Width="130" Height="32"
                        Background="#4CAF50" Foreground="White" BorderThickness="0"
                        Cursor="Hand" FontSize="13" Click="BtnRefresh_Click"
                        Margin="0,0,10,0"/>
                
                <TextBlock x:Name="txtStatus" Text="就绪" VerticalAlignment="Center"
                           Foreground="#666" Margin="10,0,0,0" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="150"/>
                <RowDefinition Height="*" MinHeight="300"/>
            </Grid.RowDefinitions>

            <!-- 数据列表 -->
            <Border Grid.Row="0" Background="White" CornerRadius="6" 
                    Padding="15" Margin="0,0,0,10">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" 
                                      ShadowDepth="1" BlurRadius="6" Opacity="0.1"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="📋 最近5天数据列表" 
                               FontSize="14" FontWeight="Bold" 
                               Foreground="#333" Margin="0,0,0,10"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                                  MaxHeight="300">
                        <StackPanel x:Name="panelDataList"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 折线图 -->
            <Border Grid.Row="1" Background="White" CornerRadius="6" 
                    Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" 
                                      ShadowDepth="1" BlurRadius="6" Opacity="0.1"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="📈 涨幅分布折线图" 
                               FontSize="14" FontWeight="Bold" 
                               Foreground="#333" Margin="0,0,0,10"/>
                    
                    <!-- 图表画布 -->
                    <Canvas x:Name="chartCanvas" Grid.Row="1" Background="White"
                            ClipToBounds="True"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 加载覆盖层 -->
        <Border x:Name="loadingOverlay" Grid.RowSpan="2" 
                Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳ 正在加载数据..." FontSize="16" 
                           Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtLoadingStatus" Text="" FontSize="12" 
                           Foreground="White" HorizontalAlignment="Center" 
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

