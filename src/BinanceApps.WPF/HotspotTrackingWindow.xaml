<Window x:Class="BinanceApps.WPF.HotspotTrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="热点追踪" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    
    <Window.Resources>
        <!-- 标题样式 -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <!-- 按钮样式 -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- 停止按钮样式 -->
        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#D13438"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- 设置和控制区 -->
            <RowDefinition Height="*"/>     <!-- 数据区域 -->
        </Grid.RowDefinitions>
        
        <!-- 设置和控制区域 -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" 
                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 第一行：参数设置 -->
                <WrapPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="量比阈值:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtVolumeRatioThreshold" Width="60" Height="28" Text="100" VerticalContentAlignment="Center"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="N天高点:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtHighPriceDays" Width="60" Height="28" Text="20" VerticalContentAlignment="Center"/>
                    <TextBlock Text="天" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="回调一区:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtPullbackZone1" Width="60" Height="28" Text="10" VerticalContentAlignment="Center"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="回调二区:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtPullbackZone2" Width="60" Height="28" Text="20" VerticalContentAlignment="Center"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="检索间隔:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtScanInterval" Width="60" Height="28" Text="5" VerticalContentAlignment="Center"/>
                    <TextBlock Text="秒" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="缓存倒计时:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtCacheExpiry" Width="60" Height="28" Text="240" VerticalContentAlignment="Center"/>
                    <TextBlock Text="小时" VerticalAlignment="Center" Margin="2,0,15,0"/>
                    
                    <TextBlock Text="流通市值:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtMinMarketCap" Width="60" Height="28" Text="0" VerticalContentAlignment="Center"/>
                    <TextBlock Text="万 ~ " VerticalAlignment="Center" Margin="2,0,2,0"/>
                    <TextBox x:Name="txtMaxMarketCap" Width="60" Height="28" Text="50000" VerticalContentAlignment="Center"/>
                    <TextBlock Text="万" VerticalAlignment="Center" Margin="2,0,0,0"/>
                </WrapPanel>
                
                <!-- 第二行：控制按钮和状态 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button x:Name="btnToggleMonitoring" Content="启动监控" Width="120" Height="32" 
                            Style="{StaticResource ButtonStyle}" Click="BtnToggleMonitoring_Click"/>
                    
                    <TextBlock x:Name="txtStatus" VerticalAlignment="Center" 
                               Margin="20,0,0,0" Foreground="#666" Text="就绪"/>
                    
                    <TextBlock x:Name="txtNextScan" VerticalAlignment="Center" 
                               Margin="20,0,0,0" Foreground="#0078D4"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 数据区域 - 平铺布局 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>  <!-- 左侧：实时监控区 -->
                <ColumnDefinition Width="3*"/>  <!-- 右侧：其他四个区域 -->
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：拆分为两个区域 -->
            <Grid Grid.Column="0" Margin="0,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>  <!-- 今日量比异动区 -->
                    <RowDefinition Height="*"/>  <!-- 实时量比监控区 -->
                </Grid.RowDefinitions>
                
                <!-- 上方：今日量比异动区 -->
                <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="1" 
                        Background="White" CornerRadius="4" Margin="0,0,0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#9C27B0" Padding="10" 
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown" 
                                Tag="dgVolumeAnomaly" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="📊 今日量比异动区" FontSize="14" FontWeight="Bold"/>
                                <Run x:Name="txtVolumeAnomalyCount" Text="(0个)" FontSize="12"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgVolumeAnomaly" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#F3E5F5"
                                  CanUserSortColumns="True"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick" FontSize="13">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="70"/>
                                <DataGridTextColumn Header="最新价" Binding="{Binding LastPrice, StringFormat=\{0:F4\}}" Width="70"/>
                                <DataGridTextColumn Header="24H涨幅" Binding="{Binding PriceChangePercent24h, StringFormat=\{0:F2\}%}" Width="70"/>
                                <DataGridTextColumn Header="24H成交额" Binding="{Binding QuoteVolume24h, StringFormat=\{0:N0\}}" Width="90"/>
                                <DataGridTextColumn Header="量比" Binding="{Binding VolumeRatio, StringFormat=\{0:F2\}%}" Width="65"/>
                                <DataGridTextColumn Header="流通市值" Binding="{Binding CirculatingMarketCap, StringFormat=\{0:N0\}}" Width="85"/>
                                <DataGridTextColumn Header="总市值" Binding="{Binding TotalMarketCap, StringFormat=\{0:N0\}}" Width="85"/>
                                <DataGridTextColumn Header="流通率" Binding="{Binding CirculatingRate, StringFormat=\{0:F2\}%}" Width="65"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
                
                <!-- 下方：实时量比监控区 -->
                <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" 
                        Background="White" CornerRadius="4" Margin="0,5,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#FF5722" Padding="10" 
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown" 
                                Tag="dgRealtime" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="🔥 实时量比监控区" FontSize="14" FontWeight="Bold"/>
                                <Run x:Name="txtRealtimeCount" Text="(0个)" FontSize="12"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgRealtime" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#FFF3F0"
                                  CanUserSortColumns="True"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick" FontSize="13">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="80"/>
                                <DataGridTextColumn Header="最新价" Binding="{Binding LastPrice, StringFormat=\{0:F4\}}" Width="75"/>
                                <DataGridTextColumn Header="24H涨幅" Binding="{Binding PriceChangePercent24h, StringFormat=\{0:F2\}%}" Width="70"/>
                                <DataGridTextColumn Header="量比" Binding="{Binding VolumeRatio, StringFormat=\{0:F2\}%}" Width="70"/>
                                <DataGridTextColumn Header="N天最高" Binding="{Binding HighPriceNDays, StringFormat=\{0:F4\}}" Width="75"/>
                                <DataGridTextColumn Header="相对高点" Binding="{Binding PriceChangeFromNDayHigh, StringFormat=\{0:F2\}%}" Width="75"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- 右侧：2x2网格 -->
            <Grid Grid.Column="1" Margin="5,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左上：量比缓存区 -->
                <Border Grid.Row="0" Grid.Column="0" BorderBrush="#E0E0E0" 
                        BorderThickness="1" Background="White" CornerRadius="4" 
                        Margin="0,0,5,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#2196F3" Padding="8"
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown"
                                Tag="dgCached" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="📦 量比缓存区" FontSize="13" FontWeight="Bold"/>
                                <Run x:Name="txtCachedCount" Text="(0个)" FontSize="11"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgCached" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#E3F2FD"
                                  CanUserSortColumns="True" FontSize="12"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="65"/>
                                <DataGridTextColumn Header="最新价" Binding="{Binding LastPrice, StringFormat=\{0:F4\}}" Width="60"/>
                                <DataGridTextColumn Header="量比" Binding="{Binding VolumeRatio, StringFormat=\{0:F1\}%}" Width="50"/>
                                <DataGridTextColumn Header="录入价" Binding="{Binding EntryPrice, StringFormat=\{0:F4\}}" Width="60"/>
                                <DataGridTextColumn Header="录入后最高" Binding="{Binding HighestPriceAfterEntry, StringFormat=\{0:F4\}}" Width="70"/>
                                
                                <!-- 录入后涨幅 - 绿色显示 -->
                                <DataGridTemplateColumn Header="录入后涨幅" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PriceGainFromEntry, StringFormat=\{0:F1\}%}" 
                                                       Foreground="Green"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <!-- 当前回撤 - 红色显示 -->
                                <DataGridTemplateColumn Header="当前回撤" Width="65">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding CurrentPullbackPercent, StringFormat=\{0:F1\}%}" 
                                                       Foreground="Red"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <DataGridTextColumn Header="录入时间" Binding="{Binding EntryTime, StringFormat=\{0:MM-dd HH:mm\}}" Width="80"/>
                                <DataGridTextColumn Header="倒计时开始" Binding="{Binding CountdownStartTime, StringFormat=\{0:MM-dd HH:mm\}}" Width="80"/>
                                <DataGridTextColumn Header="剩余(h)" Binding="{Binding RemainingCacheHours, StringFormat=\{0:F1\}}" Width="55"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
                
                <!-- 右上：回调一区 -->
                <Border Grid.Row="0" Grid.Column="1" BorderBrush="#E0E0E0" 
                        BorderThickness="1" Background="White" CornerRadius="4" 
                        Margin="5,0,0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#FF9800" Padding="8"
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown"
                                Tag="dgPullback1" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="📉 回调一区" FontSize="13" FontWeight="Bold"/>
                                <Run x:Name="txtPullback1Count" Text="(0个)" FontSize="11"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgPullback1" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#FFF3E0"
                                  CanUserSortColumns="True" FontSize="13"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="80"/>
                                <DataGridTextColumn Header="最新价" Binding="{Binding LastPrice, StringFormat=\{0:F4\}}" Width="70"/>
                                <DataGridTextColumn Header="最高价" Binding="{Binding HighestPriceAfterEntry, StringFormat=\{0:F4\}}" Width="70"/>
                                <DataGridTextColumn Header="回撤" Binding="{Binding CurrentPullbackPercent, StringFormat=\{0:F1\}%}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
                
                <!-- 左下：回调二区 -->
                <Border Grid.Row="1" Grid.Column="0" BorderBrush="#E0E0E0" 
                        BorderThickness="1" Background="White" CornerRadius="4" 
                        Margin="0,5,5,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#F44336" Padding="8"
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown"
                                Tag="dgPullback2" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="📊 回调二区" FontSize="13" FontWeight="Bold"/>
                                <Run x:Name="txtPullback2Count" Text="(0个)" FontSize="11"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgPullback2" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#FFEBEE"
                                  CanUserSortColumns="True" FontSize="13"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="80"/>
                                <DataGridTextColumn Header="最新价" Binding="{Binding LastPrice, StringFormat=\{0:F4\}}" Width="70"/>
                                <DataGridTextColumn Header="最高价" Binding="{Binding HighestPriceAfterEntry, StringFormat=\{0:F4\}}" Width="70"/>
                                <DataGridTextColumn Header="回撤" Binding="{Binding CurrentPullbackPercent, StringFormat=\{0:F1\}%}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
                
                <!-- 右下：缓存回收区 -->
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="#E0E0E0" 
                        BorderThickness="1" Background="White" CornerRadius="4" 
                        Margin="5,5,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#9E9E9E" Padding="8"
                                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown"
                                Tag="dgRecycled" Cursor="Hand">
                            <TextBlock HorizontalAlignment="Center" Foreground="White">
                                <Run Text="♻️ 缓存回收区" FontSize="13" FontWeight="Bold"/>
                                <Run x:Name="txtRecycledCount" Text="(0个)" FontSize="11"/>
                            </TextBlock>
                        </Border>
                        
                        <DataGrid x:Name="dgRecycled" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" AlternatingRowBackground="#F5F5F5"
                                  CanUserSortColumns="True" FontSize="13"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="合约" Binding="{Binding Symbol}" Width="80"/>
                                <DataGridTextColumn Header="回收时间" Binding="{Binding RecycleTime, StringFormat=\{0:MM-dd HH:mm\}}" Width="100"/>
                                <DataGridTextColumn Header="缓存时长(h)" Binding="{Binding CachedDurationHours, StringFormat=\{0:F0\}}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>

