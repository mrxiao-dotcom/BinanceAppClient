<Window x:Class="BinanceApps.WPF.RangeEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="创建范围" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 载入全部按钮 -->
        <Button Grid.Row="0" x:Name="btnLoadAll" 
                Content="📥 载入全部" 
                Width="120" Height="36"
                HorizontalAlignment="Left"
                Background="#2196F3" Foreground="White" 
                FontSize="14" FontWeight="SemiBold"
                Margin="0,0,0,15"
                Click="BtnLoadAll_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="6" BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        
        <!-- 范围名称 -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="范围名称：" 
                       VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="txtRangeName" 
                     Height="36" VerticalContentAlignment="Center" FontSize="13"
                     BorderBrush="#BDBDBD" BorderThickness="1"/>
        </Grid>
        
        <!-- 说明文字 -->
        <TextBlock Grid.Row="2" Text="合约列表（用逗号隔开）：" 
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
        
        <!-- 合约列表输入框 -->
        <Border Grid.Row="3" BorderBrush="#BDBDBD" BorderThickness="1" 
                CornerRadius="4" Background="White" Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="txtSymbolList" 
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         FontSize="12"
                         Padding="10"
                         BorderThickness="0"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </Border>
        
        <!-- 底部按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="txtStatus" 
                       VerticalAlignment="Center" 
                       Margin="0,0,15,0" 
                       FontSize="12"
                       Foreground="#666666"/>
            <Button x:Name="btnSave" Content="💾 保存" 
                    Width="100" Height="36" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" 
                    FontSize="14" FontWeight="SemiBold"
                    Click="BtnSave_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="btnCancel" Content="取消" 
                    Width="100" Height="36"
                    Background="#9E9E9E" Foreground="White" 
                    FontSize="14" FontWeight="SemiBold"
                    Click="BtnCancel_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>

